#include <stdio.h>
#include <stdlib.h>
#define tam 150
int main()
{

    int no,boy, fark,min_boy,minno;

    printf("ogrenci no:");
    scanf("%d",&no);
    printf("ogrenci boy:");
    scanf("%d",&boy);

    minno=no;
    fark=abs(boy-tam);

    while()



    return 0;
}
